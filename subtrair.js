// Declaração
// 1° Parâmetro = a a ser subtraido
// 2° Parâmetro = b a ser subtraido
function subtrair(a, b){
    const resultado = a - b;
// retorna a subtração dos dois números    
    return resultado;
}

// Exportação da função subtrair
module.exports = subtrair;

