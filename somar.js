// Declaração
// 1° Parâmetro = a a ser somado
// 2° Parâmetro = b a ser somado
function somar(a, b){
    const resultado = a + b;
// retorna a soma dos dois números    
    return resultado;
}

// Exportação da função somar
module.exports = somar;

